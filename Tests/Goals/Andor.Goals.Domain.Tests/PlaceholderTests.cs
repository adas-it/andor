namespace Andor.Goals.Domain.Tests;

// Placeholder so the CI/Sonar pipeline for the Goals module has a test project to run
// against until the first real aggregate lands. Delete once real domain tests exist.
public class PlaceholderTests
{
    [Fact]
    public void Placeholder_ShouldPass()
    {
        Assert.True(true);
    }
}
